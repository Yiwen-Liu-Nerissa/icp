import java.util.Scanner;

class Main {
 public static void main(String[] args) {
   Scanner input = new Scanner(System.in);
   String word = input.nextline();
   
   for (int lettersInWord = word.charAt(letters);
   System.out.printIn(letterInWord);
  }

 }
}
